# Coupled launch-lane handling — test record

- `tests/m13.11-lane-coupled.js`: 20 valid native starts and 80 polygon TOI impacts across the sampled frame states; maximum position error **0.0000731 m**, velocity error **0.0000248 m/s**. Fourteen 120-frame lane traces (1,680 frames total), with 1–40 m/s initial velocities, had no escape within the protected interval, up to eight impacts per frame, and no TOI-budget exhaustion.
- `tests/m13.9-boundaries.js` continued to check 35 playfield-to-divider starts, 15 legal passes above the divider tip, 15 central-ceiling states and older right-side cases as diagnostics; wall-overlap initial states were not retroactively counted as parity.
- `tests/m13.10-upper-bumper.js` protected nine bumper sequences (405 frames) and five isolated curve/ceiling comparisons.
- The 34-test JavaScript suite and a managed Chromium gate passed at that stage. Chromium used exact inline HTML/CSS/JS/media when localhost and file access were denied; physical-device acceptance was not implied.
- At that historical revision `js/physics.js` was the only changed runtime file. The 23 archived files and other runtime modules were unchanged.
