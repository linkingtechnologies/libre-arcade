# Tests

Run from the repository root with:

```bash
npm test
```

The suite is dependency-free and uses Node's built-in test runner. It covers historical rules, deterministic/scripted randomness, board integrity, full CPU-only smoke games, the original AI priority structure and priority-2 threat-analysis bug, player-facing copy safeguards, and clean-room audio event mapping.
