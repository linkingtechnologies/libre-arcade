# Upstream reference status

Upstream project: Dwarf City / Bubble Train, Adam Child and Craig Marshall. Historical homepage: `http://dwarfcity.co.uk/games/bubbletrain.php` (no longer relied on as a live download).

A pristine upstream release archive has **not yet been recovered into this repository**. The OS4 package contains the C++ source tree and runtime data, but OS4 porter changes are mixed into that source snapshot. Keep this directory as the destination for an upstream archive when recovered; hash it before extraction.
