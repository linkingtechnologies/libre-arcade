# Archaeology record

Upstream project: `rriesebos/game-of-the-goose` by Robert Riesebos.

Preserved/audited GitHub `main` snapshot commit: `e8b804ffd3baa999416409fdec98cfc9e01ce87b`.

The upstream `package.json` declares `ISC`. The audited snapshot contains no standalone `LICENSE`, `COPYING`, `NOTICE`, or source license headers. The complete untouched original snapshot is preserved in `reference/game-of-the-goose-e8b804f/` (fetched independently from the live GitHub repository and verified against the GitHub API's own git blob hashes — see `reference/README.md`) and is not loaded by the standalone runtime because some visual/vendored components lack provenance notices.

Quarantined from this standalone distribution:

- `img/goose.svg` and `img/player0.svg` … `player5.svg`;
- `src/roll-a-die/`;
- unattributed inline SVG icon paths;
- upstream screenshots containing the quarantined visuals.

The runtime replacements in this standalone package are CSS/JS/text primitives created for the restoration.
