# Reference and rights policy

1. Historical package supplied by the user: **HighMoon 1.2.4**, received as an uncompressed TAR (686,080 bytes), SHA-256 `c0a4e71ce83323063df53104aaa46fb3488fa6712d2b7a4827cca48161494f0b`. The historical gzip wrapper recorded by FreeBSD has SHA-256 `4363ea2455869b886b5777272cddd69ac0a14c7635cdbdafba31eb1c6625bdc6`, size 413,137 bytes. **The gzip wrapper was not received or reconstructed.**
2. `reference/reference/HighMoon/` in the public tree contains **only byte-identical original source and release docs** (not a complete replacement for the original TAR). See `ORIGINAL_FILE_MANIFEST.json` for per-file SHA and public/private classification.
3. The separately supplied `highmoon-original-private-reference.zip` contains the exact received TAR, unchanged full extracted tree, and SHA manifest. Store it outside Git/GitHub Pages. It is NOT intended to be copied wholesale into a public repo.
4. Source header grants **GPL-2.0-or-later**; our new JS game is under GPLv3 (see `LICENSE`). The upstream `COPYING` text is preserved alongside the upstream source.
5. Original `gfx/*`, `snd/*`, `icon.png` have unresolved file-by-file third-party provenance; the project-level license is not conclusive for each. They are retained in the private historical archive but excluded from the public deploy and public commit package. Seven WAVs have OpenOffice-family ancestry and `click.wav` has an older QNetWalk match; see `specs/audit-phase4/`. This segregation is a conservative distribution choice, not a determination that archival possession is itself forbidden.
6. Gzipped JSONL traces are **lossless**, not altered test results. `gzip -dc file.jsonl.gz` reconstructs their original contents. The native renderer stub reads GIF dimensions, so a fresh native run needs the withheld original tree restored locally; the JS test suite uses embedded summary fixtures and needs no historical media.
7. All URLs and assertions concerning third-party provenance in the audits are historical evidence, **not blanket permissions to redistribute the third-party WAVs or planet textures**. `reference` is not a generic asset directory for the public game.
